# Copie para .env.local e preencha. NUNCA versione o .env.local.
# Apenas a chave PUBLICAVEL entra no cliente (spec §21.4).
VITE_FLY_ENVIRONMENT=local
VITE_FLY_SUPABASE_URL=
VITE_FLY_SUPABASE_PUBLISHABLE_KEY=
VITE_FLY_APP_VERSION=0.1.0
# Injetado pela esteira de build; deixe vazio localmente.
VITE_FLY_COMMIT_SHA=
